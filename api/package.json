{
  "name": "express-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --watch server",
    "dev": "node --watch --env-file=.env server"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
